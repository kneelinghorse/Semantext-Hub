{
  "name": "@ossp/openapi-adapter",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Reference OpenAPI adapter for OSSP Pack v1",
  "scripts": {
    "build": "node ./src/index.mjs --spec ./fixtures/spec.json --out ../../artifacts/adapters/openapi",
    "run": "node ./src/index.mjs",
    "test": "node --test ./tests"
  }
}
