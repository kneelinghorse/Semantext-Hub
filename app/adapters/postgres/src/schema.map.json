{
  "$schema": "https://ossp.dev/schemas/adapter-schema-map.json",
  "adapter": "postgres",
  "type": "data",
  "source": "./fixtures/minimal.json",
  "notes": [
    "Update this map as you align the source specification with catalog primitives."
  ],
  "mappings": [
    {
      "from": "$.tables[*]",
      "to": "$.catalog.data.entities[]",
      "description": "Map table definitions to catalog data entities."
    }
  ]
}
