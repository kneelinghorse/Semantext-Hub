{
  "private": true,
  "name": "oss-protocols-root",
  "description": "Root package wrapper for OSS Protocols workspace",
  "scripts": {
    "test": "npm --prefix app test"
  },
  "dependencies": {
    "@asyncapi/parser": "^3.4.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0"
  }
}
