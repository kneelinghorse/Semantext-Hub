#!/usr/bin/env node

console.warn('protocol-generate has been replaced by the unified `sch` CLI scaffolding workflow.');
console.warn('Command stubs live under `sch protocol ...` and backlog items are tracked in docs/operations/cli-backlog.md.');
process.exitCode = 1;
